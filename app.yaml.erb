beta_settings:
  cloud_sql_instances: <%= ENV["CLOUD_SQL_CONNECTION_NAME"] %>
entrypoint: bundle exec rackup --port $PORT
env: flex
env_variables:
  SECRET_KEY_BASE: <%= ENV["SECRET_KEY_BASE"] %>
  YARDSTICK_DATABASE_HOST: <%= ENV["YARDSTICK_DATABASE_HOST"] %>
  YARDSTICK_DATABASE_PASSWORD: <%= ENV["YARDSTICK_DATABASE_PASSWORD"] %>
runtime: ruby
